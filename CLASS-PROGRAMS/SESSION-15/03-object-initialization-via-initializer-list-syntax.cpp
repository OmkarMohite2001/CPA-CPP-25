#include <iostream> 

class Date 
{
    private: 
        int day; 
        int month; 
        int year; 
    public: 
        Date(int init_day, int init_month, int init_year) : day(init_day), month(init_month), 
                                                            year(init_year)
        {

        } 
}; 

LHS = RHS; 

Date() : member_name(rhs_expr)

int num = get_next_integer(); 

Date() : num (get_next_integer())

struct Date myDate = {9, 11, 2025}; 

Date myDate(9, 11, 2025); 